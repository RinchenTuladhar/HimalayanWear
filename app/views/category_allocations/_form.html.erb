<%= form_with(model: category_allocation, local: true) do |form| %>
  <% if category_allocation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category_allocation.errors.count, "error") %> prohibited this category_allocation from being saved:</h2>

      <ul>
        <% category_allocation.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.number_field :category_id %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.number_field :product_id %>
  </div>

  <div class="actions">
    <%= form.submit :class => "white-btn" %>
  </div>
<% end %>
